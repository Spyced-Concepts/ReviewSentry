# UAT Template — ai-pr-review

> **Do not edit this file for a test run.** Copy it to `docs/uat/runs/uat-<version>-<platform>.md` on a dedicated `uat/<version>-<platform>` branch. Each run is a discrete record with its own commit trail.

---

## Run metadata

| Field | Value |
|---|---|
| Version | <!-- e.g. v0.2.0-alpha --> |
| Platform | <!-- macbook-pro / windows-desktop / zorin-laptop --> |
| Tester | <!-- Stu Last --> |
| Date | <!-- YYYY-MM-DD --> |
| Action ref tested | <!-- e.g. Spyced-Concepts/ai-pr-review@functional-test --> |
| Provider(s) tested | <!-- anthropic / openai / gemini / github-models --> |
| Test repo | <!-- e.g. Spyced-Concepts/corvex-strike --> |
| Branch | <!-- uat/v0.2.0-alpha-macbook --> |

## Result summary

| Result | Count |
|---|---|
| ✅ Pass | |
| ❌ Fail | |
| ⏭️ Skip | |

**Overall:** <!-- PASS / FAIL / PARTIAL -->

---

## Prerequisites checklist

- [ ] Action ref is the correct branch/tag under test
- [ ] Test repo has `AI_API_KEY` secret configured
- [ ] Test repo has `AI_MODEL` variable configured
- [ ] Test repo has `ai-review.yml` workflow pointing at the action ref under test
- [ ] Tester has access to create PRs and observe workflow runs in the test repo

---

## Test scenarios

### UAT-001 — Valid diff, Anthropic provider

**Setup:** Open a PR in the test repo with at least one changed file and a clean diff (no intentional errors).

**Expected:** Workflow completes successfully. A comment starting with `## AI Code Review` is posted on the PR. Review ends with `APPROVE`, `APPROVE WITH NOTES`, or `REQUEST CHANGES`.

| Check | Result | Notes |
|---|---|---|
| Workflow run completes without error | | |
| Review comment posted on PR | | |
| Comment contains `## AI Code Review` header | | |
| Comment ends with one of the three verdicts | | |
| `*Automated review. Maintainer approval required.*` footer present | | |

**Result:** <!-- ✅ Pass / ❌ Fail / ⏭️ Skip -->
**Notes:** <!-- any observations -->

---

### UAT-001b — Valid diff, OpenAI provider

**Setup:** Same PR as UAT-001, or a new PR. Change workflow `ai_provider` to `openai` and update `AI_MODEL` to an OpenAI model (e.g. `gpt-4o`).

**Expected:** Same as UAT-001 — review comment posted successfully.

| Check | Result | Notes |
|---|---|---|
| Workflow run completes without error | | |
| Review comment posted on PR | | |
| Comment structure identical to Anthropic output | | |

**Result:** <!-- ✅ Pass / ❌ Fail / ⏭️ Skip -->
**Notes:**

---

### UAT-001c — Valid diff, GitHub Models (zero cost)

**Setup:** Change workflow to use `ai_api_key: ${{ secrets.GITHUB_TOKEN }}`, `ai_provider: openai`, `ai_base_url: https://models.inference.ai.azure.com`, `ai_model: gpt-4o`. No extra secret needed.

**Expected:** Review comment posted using the GitHub-provided token. No additional API key required.

| Check | Result | Notes |
|---|---|---|
| Workflow run completes without error | | |
| Review comment posted on PR | | |
| No external API key used | | |

**Result:** <!-- ✅ Pass / ❌ Fail / ⏭️ Skip -->
**Notes:**

---

### UAT-002 — Large diff truncation note

**Setup:** Open a PR with a diff exceeding 1500 lines (or set `diff_lines: 10` to simulate truncation with any small diff).

**Expected:** Review comment is posted. The comment contains a truncation note: `> Diff was large — review based on first N lines only.`

| Check | Result | Notes |
|---|---|---|
| Workflow run completes without error | | |
| Truncation note present in comment | | |
| Note references the correct line limit | | |

**Result:** <!-- ✅ Pass / ❌ Fail / ⏭️ Skip -->
**Notes:**

---

### UAT-003 — Missing AI_API_KEY

**Setup:** Temporarily remove or rename the `AI_API_KEY` secret in the test repo. Open a PR or re-run a workflow.

**Expected:** Workflow fails. Log contains `::error::AI_API_KEY secret not configured`.

| Check | Result | Notes |
|---|---|---|
| Workflow fails (non-zero exit) | | |
| Error message is clear and actionable | | |
| No partial review posted | | |

**Result:** <!-- ✅ Pass / ❌ Fail / ⏭️ Skip -->
**Notes:**

---

### UAT-004 — Missing AI_MODEL

**Setup:** Remove or rename the `AI_MODEL` variable. Open a PR or re-run.

**Expected:** Workflow fails with `::error::AI_MODEL variable not configured`.

| Check | Result | Notes |
|---|---|---|
| Workflow fails (non-zero exit) | | |
| Error message is clear and actionable | | |

**Result:** <!-- ✅ Pass / ❌ Fail / ⏭️ Skip -->
**Notes:**

---

### UAT-005 — Invalid API key (HTTP error from provider)

**Setup:** Set `AI_API_KEY` to a syntactically valid but invalid key (e.g. `sk-ant-INVALID000000000000`).

**Expected:** Workflow fails with an HTTP error message from the provider (e.g. `::error::API HTTP error 401: ...`).

| Check | Result | Notes |
|---|---|---|
| Workflow fails (non-zero exit) | | |
| HTTP error code visible in log | | |
| No partial review posted | | |

**Result:** <!-- ✅ Pass / ❌ Fail / ⏭️ Skip -->
**Notes:**

---

### UAT-006 — Unknown AI_PROVIDER value

**Setup:** Set `ai_provider: notarealai` in the workflow.

**Expected:** Workflow fails with `::error::Unknown AI_PROVIDER 'notarealai'. Supported: anthropic, gemini, openai`.

| Check | Result | Notes |
|---|---|---|
| Workflow fails (non-zero exit) | | |
| Error lists all supported providers | | |

**Result:** <!-- ✅ Pass / ❌ Fail / ⏭️ Skip -->
**Notes:**

---

### UAT-007 — Sensitive data detected in diff

**Setup:** Open a PR that includes a dummy API key pattern in the diff, e.g. add a line: `# API_KEY=sk-ant-test1234567890abcdefghij` to a file.

**Expected:** Review comment is posted. Criterion 1 (Sensitive data disclosure) flags the key pattern as Critical severity. The finding appears before any other findings.

| Check | Result | Notes |
|---|---|---|
| Review comment posted | | |
| Sensitive data finding reported as criterion 1 | | |
| Severity classified as Critical | | |
| Finding appears before other criteria | | |

**Result:** <!-- ✅ Pass / ❌ Fail / ⏭️ Skip -->
**Notes:**

---

### UAT-008 — PR body with shell metacharacters

**Setup:** Open a PR where the PR body contains shell metacharacters: `$(echo injected)` and backtick expressions.

**Expected:** Review completes normally. No injection occurs. The metacharacters appear safely in the review if referenced at all.

| Check | Result | Notes |
|---|---|---|
| Workflow run completes without error | | |
| No unexpected shell execution in logs | | |
| Review comment posted normally | | |

**Result:** <!-- ✅ Pass / ❌ Fail / ⏭️ Skip -->
**Notes:**

---

### UAT-009 — Custom review criteria appended

**Setup:** Add `review_criteria: "Check for hardcoded TODO comments"` to the workflow step. Open a PR.

**Expected:** Review comment is posted. The custom criterion appears in the review output.

| Check | Result | Notes |
|---|---|---|
| Review comment posted | | |
| Custom criterion visible in review | | |

**Result:** <!-- ✅ Pass / ❌ Fail / ⏭️ Skip -->
**Notes:**

---

### UAT-010 — Custom rules (sensitive data scan)

**Setup:** Add `custom_rules: "INTERNAL_PROJECT_NAME"` to the workflow step. Open a PR that includes the string `INTERNAL_PROJECT_NAME` in the diff.

**Expected:** The sensitive data scan flags the custom term as a finding.

| Check | Result | Notes |
|---|---|---|
| Custom term flagged in review | | |
| Finding appears in criterion 1 section | | |

**Result:** <!-- ✅ Pass / ❌ Fail / ⏭️ Skip -->
**Notes:**

---

## Sign-off

| Field | Value |
|---|---|
| Tester | |
| Date | |
| Overall result | |
| Ready to merge to functional-test | <!-- Yes / No --> |
| Notes / defects raised | |
